import React from 'react';
import ReactDOM from 'react-dom';
import MsToBeats from './MsToBeats';

ReactDOM.render(
  <React.StrictMode>
    <MsToBeats />
  </React.StrictMode>,
  document.getElementById('root')
);
